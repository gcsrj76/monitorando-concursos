package com.palinux.monitorandoconcursos.ui.theme

import androidx.compose.ui.graphics.Color

// Tons de Preto e Azul Escuro (Para Fundos e Elementos Principais)
val AzulEscuroProfundo = Color(0xFF0B121F) // Fundo principal da tela
val AzulNoite = Color(0xFF152033)          // Fundo de Cards e Containers
val PretoAsfalto = Color(0xFF070A0F)       // TopAppBar e superfícies escuras

// Tons de Destaque (Componentes Ativos, Botões e Seleções)
val AzulAco = Color(0xFF3F69A8)            // Botões principais e links
val AzulClaroFocado = Color(0xFF7097D1)    // Estados de foco e textos de destaque

// Tons de Cinza (Para Textos e Bordas)
val CinzaClaro = Color(0xFFE2E8F0)         // Texto principal (Alta legibilidade)
val CinzaMutado = Color(0xFF94A3B8)        // Texto secundário (Legendas e rótulos)
val CinzaBorda = Color(0xFF334155)         // Bordas de inputs e divisores